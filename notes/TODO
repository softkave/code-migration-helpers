# TODO

# Trunk

- show skipped files and why
- write tests for handling large folders

# Add `vitest` to tests

- how to handle existing imports from "node:test" or other imports

# Add index file

- using/merging with an existing export declaration
  - import then export occurrences
- index file extension OR default to first entry file extension
- explicit vs full (\*) exports when exporting every export
- allow user specify index filename
- support module.exports
